import 'package:flutter/material.dart';

class GosendWebviewScreen extends StatelessWidget {
  const GosendWebviewScreen({Key? key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
